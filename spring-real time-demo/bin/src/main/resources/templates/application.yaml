  spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers: localhost:9092
      bindings:
        menu-in:
          destination: menu
          contentType: application/json
        menu-out:
          destination: menu
          contentType: application/json
          